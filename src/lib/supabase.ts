// supabase.ts — safely stubbed out.
// The app now uses localStorage for all data persistence (see src/lib/store.ts).
// This file is kept to prevent any legacy import errors but exports nothing active.
export {}
